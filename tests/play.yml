---
# Test for custom ovh modules

- hosts: localhost
  tasks:
    - name: Test with a message
      ovh_server:
        service: ns553569.ip-142-44-212.net
        template: foo
        hostname: bar
        ssh_key: pub
        distrib_kernel: true
